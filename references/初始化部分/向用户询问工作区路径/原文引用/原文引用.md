# EasyEDAssistant Reference：向用户询问工作区路径 — 原文引用

> 本文件收录「向用户询问工作区路径」的原文引用（正本：`references/lib/connection-setup.md` §1.1）。

## 原文引用（`connection-setup.md` §1.1）

> - **确认工作区根目录（cwd）**：端口连通后，询问用户其 EDA 工程
>   （原理图/PCB 文件所在目录）的绝对路径，与 Kilocode 会话的
>   `workspace root` 比对；以用户指定路径作为本会话运行时根目录
>   `cwd`（所有运行产物 `./tmp/`、截图、回读 JSON 的根目录）。
>   用户指定路径与 Kilocode `workspace root` 不一致时以用户指定为准，
>   并提示确认当前会话的工作区是否与该路径匹配。

> 注：原文章节在链路（端口）连通后执行该询问；本"初始化部分"将该动作提前为
> 纯工作区初始化步骤，询问与路径确认本身不依赖 EDA 连接。