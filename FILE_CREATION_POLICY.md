# 新建文件与文件夹使用规范 (Kilo Model 安全合规)

> **注意**：本文件旨在规范 Kilo Model 在 EasyEDAssistant 项目中新创文件与文件夹的行为，以防止因模型工具滥用而导致的仓库污染或安全风险。请严格遵循本规范执行。

---

## 1. 概述

### 1.1 目的
- 防止 Kilo Model 通过文件工具 (`read`、`write`、`edit` 等) 随意创建或访问未经授权的文件/文件夹
- 明确项目的可写范围，保障仓库健康与安全
- 为模型提供明确的操作边界，减少误操作风险

### 1.2 适用范围
- 所有在 `C:\Users\User\.kilocode\skills\EasyEDAssistant` 目录下运行的 Kilo Model 会话
- 所有新创文件或文件夹的操作，包括临时文件、日志、脚本、文档等
- 涉及路径访问、权限变更或内容修改的任何模型操作

---

## 2. 新建文件范围

### 2.1 允许新建的文件类型

| 目录 | 文件类型 | 限制说明 |
|------|----------|----------|
| 项目根目录 | `.md` 文件 | 仅用于项目文档、规范、协议等；不得创建个人笔记或临时草稿 |
| `scripts/` | `.py` 脚本文件 | 必须遵循项目架构（如 `tool-probe.py`、`tool-probe-simulator.py` 等）；新增脚本需经过安全审核 |
| `Sample/easyeda-agent/` | `.md`、`.json` 文件 | 仅用于参考文档、数据文件或配置文件；不得包含敏感信息 |
| `agents/` | `.yaml`、`.json` 文件 | 仅用于 Agent 定义；需经过权限审批 |
| `Sample/easyeda-agent/references/` | `.md`、`.json` 文件 | 仅用于参考文档、设计流程、数据文件；不得修改核心规则 |

### 2.2 禁止新建的文件类型

| 禁止新建的文件类型 | 原因 |
|---|---|
| 任意 `.txt`、`.log` 文件 | 可能用于日志记录或临时存储，导致版本控制污染 |
| 任意 `.tmp`、`.temp` 文件 | 临时文件易导致垃圾文件堆积 |
| `~/.kilo/` 相关文件 | 环境注入文件，不应成为仓库内容 |
| 任意 `.pyc`、`__pycache__/` 文件 | 缓存文件不应提交到 Git |
| `*.swp`、`*~` 文件 | 编辑器临时文件 |
| 任意 `*.bak`、`*.old` 文件 | 备份文件可能包含敏感信息 |

### 2.3 文件命名规范

- **必须**以项目用途为名称，不能使用随机或模糊名称
- **不得**使用超出当前任务范畴的名称（如个人临时文件、测试文件等）
- **必须**在文件名中体现文件用途，便于团队审阅

---

## 3. 新建文件夹范围

### 3.1 允许新建的文件夹类型

| 目录 | 文件夹类型 | 限制说明 |
|------|----------|----------|
| 项目根目录 | **无新增文件夹权限** | 所有内容应写在现有文档中，不得创建子文件夹 |
| `scripts/` | 功能子文件夹 | 仅在 `scripts/` 下创建明确功能子文件夹（如 `scripts/utils/`、`scripts/test/`）；需经过批准 |
| `Sample/easyeda-agent/` | 仅参考子文件夹 | 不得在 `Sample/easyeda-agent/` 下创建新文件夹，仅可修改现有结构 |
| `agents/` | 仅定义文件存储 | 不得在 `agents/` 下创建新文件夹，仅存储 `.yaml`、`.json` 文件 |

### 3.2 禁止新建的文件夹类型

| 禁止新建的文件夹 | 原因 |
|---|---|
| 项目根目录下的任意子文件夹 | 破坏文档结构，造成审阅困难 |
| `Sample/easyeda-agent/` 下的任意子文件夹 | 防止未经批准的文件树扩展 |
| `agents/` 下的任意子文件夹 | 避免 Agent 配置分散 |
| `Sample/easyeda-agent/references/` 下的子文件夹 | 除非项目明确需要，否则不允许 |
| 任意临时工作目录 | 可能导致环境污染 |

---

## 4. 新建文件与文件夹的操作流程

### 4.1 标准操作流程

1. **需求分析**
   - 确定是否需要新文件或文件夹
   - 评估是否符合项目架构
   - 确认是否有相关权限

2. **方案设计**
   - 拟定文件/文件夹的结构
   - 确定路径、权限和版本控制策略
   - 记录变更需求

3. **审批流程**
   - 对于重大变更（如新增功能子文件夹），需经过项目负责人批准
   - 对于文档更新，需经过代码评审

4. **创建操作**
   - 使用 `write` 或 `edit` 工具进行文件创建或修改
   - 对于文件夹，无法直接创建，需通过文件路径来间接创建
   - 确保文件的权限设置符合项目要求

5. **验证与审核**
   - 检查文件内容是否符合项目标准
   - 验证路径是否正确
   - 确保无遗留临时或不必要文件

6. **版本控制**
   - 新文件或文件夹必须添加到 `.gitignore` 中（如临时文件）
   - 正常文件需提交到 Git

### 4.2 特殊情况处理

#### 4.2.1 紧急修复
如果在项目运行中发现严重问题，需要紧急修复，允许直接创建临时文件，但必须在修复后立即清理。

#### 4.2.2 文档更新
更新 `Sample/easyeda-agent-skill-behavior.md` 等文档时，必须遵循项目文档结构，不得随意添加新章节或子章节。

#### 4.2.3 脚本修改
修改现有脚本时，必须在不破坏原有功能的基础上进行，不得随意重构脚本结构。

---

## 5. 安全与合规要求

### 5.1 文件内容安全
- 不得在文件中包含敏感信息（如 API 密钥、密码、内部文档等）
- 文件内容需经过安全审查

### 5.2 路径安全
- 不得访问或创建超出项目范围的路径
- 所有文件操作应限制在项目根目录内

### 5.3 权限控制
- 对于新文件或文件夹，应设置适当的权限限制
- 确保不会因权限过低导致操作失败或安全漏洞

### 5.4 版本控制
- 新文件需在 Git 中妥善处理
- 对于临时文件，应确保它们被正确地添加到 `.gitignore` 中

---

## 6. 违规处理

### 6.1 违规行为定义
- 未经批准创建不必要文件或文件夹
- 违反命名规范的文件或文件夹
- 包含敏感信息或不符合项目标准的文件
- 超出项目范围的文件操作

### 6.2 处理措施
- 立即停止相关操作
- 恢复文件或文件夹到安全状态
- 对违规操作者进行通报批评
- 根据情节轻重进行相应的处罚

---

## 7. 附则

### 7.1 术语表
- **Kilo Model**：指运行在 Kilo 平台上的模型
- **Agent**：指 EasyEDAssistant 项目的智能体
- **Skill**：指 EasyEDAssistant 项目的技能定义
- **工具**：指 `read`、`write`、`edit`、`glob`、`bash` 等 Kilo 提供的工具

### 7.2 修订历史
- **2026-09-14**：初始版本发布
- **后续版本**：根据项目发展进行修订

---

## 8. 结束语

本规范旨在为 Kilo Model 提供明确的操作边界，防止因滥用文件工具而导致的仓库污染或安全风险。请严格遵守本规范执行，如有任何疑问，请及时咨询项目负责人。

---

**制定人：** Kilo Team  
**生效日期：** 2026-09-14  
**版本：** 1.0  
