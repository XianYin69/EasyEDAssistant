# 新建文件与文件夹使用规范（Kilo Model 安全合规）

> 本文件只约束**大模型在设计执行期使用文件的范围、工作区的范围及权限**；细则正本在 [`references/约束部分/约束部分.md`](references/约束部分/约束部分.md)。
> 编辑 skill 本体文档遵守 [`RULE_EDIT.md`](RULE_EDIT.md)，不受本文件白名单限制。

## 1. 概述

### 1.1 目的
防止模型通过文件工具（`read`/`write`/`edit` 等）越界创建或访问未经授权的文件与文件夹；明确设计执行期的可写范围与权限边界，保障仓库健康与工程隔离。

### 1.2 适用与不适用
- **适用**：设计执行期（原理图 / PCB 开发）在**工程工作区**（`cwd`）内创建、读写、清理文件与目录。
- **不适用**：编辑 skill 本体（`SKILL.md`、`references/`、`scripts/`、`Sample/`、`CHANGELOG.md`、`RULE_EDIT.md`）。该场景遵守 `RULE_EDIT.md`（≤50 行、自然语言、总索引 + 子文件、改 CHANGELOG 并提交 git）。

### 1.3 skill 目录 vs 工作区（关键概念）
- **skill 目录**（如 `~/.kilocode/skills/EasyEDAssistant/`）是技能定义与版本库，**只读 + 受版本控制**，运行产物不得写入。
- **只读不止文件工具**：设计执行期同样禁止经由命令通道改写 skill——不得跑 `easyeda update`（含 `--skill-only`）、`easyeda skill sync/status`；启动 daemon 必须 `easyeda daemon start --auto-update-skill=false`（该参数默认 on，会静默覆写已安装 skill 目录）。正本见 `references/约束部分/运行环境不可变/`。
- **工作区目录**（执行时的 `cwd`，即当前 EDA 工程所在目录）是运行时活动根，运行产物只允许写在这里。
- 判据：`Path(__file__).parent` 等「脚本自身位置」派生路径指向 skill 目录，禁止用作运行产物根；运行产物必须以 `Path.cwd()` 或相对路径（`./tmp/`）解析到工作区。

## 2. 文件使用范围

### 2.1 允许范围
工作区 `./tmp/` 及其按用途划分的子目录（类型与目录白名单正本见 [`文件与文件夹创建范围`](references/约束部分/文件与文件夹创建范围/文件与文件夹创建范围.md)）；工作区根目录的项目交付文档（技术手册、功能手册、交付报告）与用户指定的正式交付目录。

### 2.2 禁止范围
skill 目录内的 `tmp/` 或任何运行产物目录；工作区之外的任意目录；被禁格式与缓存产物（`.txt`/`.log`/`.tmp`/`.bak`/`*.pyc`/`__pycache__/`/编辑器 swap/`.kilo/` 注入文件）；含密钥、凭据或内部敏感信息的文件。

### 2.3 运行产物落位与版本控制
`./tmp/` 必须建在工作区并以相对路径解析，且写入工作区 `.gitignore`，任何 tmp 内容不得提交 git；脚本输出根默认值必须是 `./tmp`（相对 `cwd`），不得用 `Path(__file__).parent` 作默认根。

## 3. 路径安全与防止逃逸（Path Traversal Guard）
- 所有读写与创建限定在 `Path.cwd()` 及其子目录；不得用 `../` 或绝对路径跨出工作区、触及系统敏感目录。
- 接受外部路径或工程名参数（`--project`/`--doc`/`--output-dir`）的脚本必须清洗：以 `Path(p).name` 剥离分隔符与 `..`，解析后的绝对路径须通过 `is_relative_to(Path.cwd())` 校验，未通过即抛 `PermissionError` 并拒绝落笔。
- 不把未清洗的用户字符串直接拼接进路径。

## 4. 权限与审批
- 新建目录、新增或重构脚本属受审批动作：新增脚本须安全审核，重大结构变更须项目负责人批准。
- 破坏性或不可逆动作（覆盖既有交付物、把临时产物长期保留入库、放宽格式白名单）须用户明确授权；无授权即停止并询问。
- 按目录契约新建 tmp 子目录、按 [`垃圾处理`](references/约束部分/垃圾处理/垃圾处理.md) 清理产物不需逐次请示。

## 5. 细则索引（正本在 `references/约束部分/`）
- 类型与目录白名单 [`文件与文件夹创建范围`](references/约束部分/文件与文件夹创建范围/文件与文件夹创建范围.md)（含 skill 目录运行期只读）、操作流程 [`创建操作流程`](references/约束部分/创建操作流程/创建操作流程.md)、内容安全与合规 [`安全与合规`](references/约束部分/安全与合规/安全与合规.md)、运行环境不可变 [`运行环境不可变`](references/约束部分/运行环境不可变/运行环境不可变.md)。
- 命名 [`文件命名规范`](references/约束部分/文件命名规范/文件命名规范.md)、格式 [`文件存储格式`](references/约束部分/文件存储格式/文件存储格式.md)、清理 [`垃圾处理`](references/约束部分/垃圾处理/垃圾处理.md)、压缩 [`上下文存储压缩机制`](references/约束部分/上下文存储压缩机制/上下文存储压缩机制.md)。
- 证据链 [`记忆链`](references/约束部分/记忆链/记忆链.md)、推导链 [`逻辑链的存储`](references/约束部分/逻辑链的存储/逻辑链的存储.md)、处置 [`处罚机制`](references/约束部分/处罚机制/处罚机制.md)／[`激励机制`](references/约束部分/激励机制/激励机制.md)。

## 6. 修订历史
- **v2.1（2026-09-18）**：§1.3 增「skill 只读不止文件工具」（禁 `easyeda update`/`skill sync`、daemon 必带 `--auto-update-skill=false`），§5 补 `运行环境不可变` 指针。**v2.0/v1.3（2026-09-16）**：按 RULE_EDIT 压到 ≤50 行，各细则迁入 `references/约束部分/`，§1.2 明确只约束设计执行期工作区文件，`references/` 允许按「总索引 + 子文件」建子文件夹。更早版本见 `CHANGELOG.md`。

**制定人：** Kilo Team ｜ **生效日期：** 2026-09-14 ｜ **版本：** 2.1